{% extends 'base.html.twig' %}

{% block title %}Créer un Panier{% endblock %}

{% block body %}
<h1>Créer un Panier</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">Créer</button>
{{ form_end(form) }}

<a href="{{ path('app_panier_index') }}" class="btn btn-secondary mt-2">⬅ Retour à la liste</a>
{% endblock %}
